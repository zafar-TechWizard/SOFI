# Core Framework
groq>=0.4.1
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Async and HTTP
httpx>=0.25.2
aiohttp>=3.9.0
aiofiles>=23.2.1
websockets>=12.0
asyncio-mqtt>=0.16.1

# Database and Storage
sqlalchemy>=2.0.23
alembic>=1.13.1
psycopg2-binary>=2.9.9
redis>=5.0.1
chromadb>=0.4.18
neo4j>=5.15.0

# Data Processing and Analysis
numpy>=1.24.3
pandas>=2.1.4
matplotlib>=3.8.2
seaborn>=0.13.0
plotly>=5.17.0
scikit-learn>=1.3.0

# Web Scraping and Content Processing
beautifulsoup4>=4.12.2
requests>=2.31.0
lxml>=4.9.3
ddgs>=0.1.0

# AI and ML (Groq only - no other AI providers)
sentence-transformers>=2.2.2
# Note: Removed openai, langchain, transformers, torch to ensure Groq-only usage
# Only keeping sentence-transformers for local embeddings if needed

# NLP — Memory System Entity Extraction
# spaCy + English model for NER, noun-chunk extraction
spacy>=3.7.0
# coreferee: pronoun resolution ("she", "him", "that") via spaCy pipeline
coreferee>=1.4.0
# GLiNER: zero-shot NER with custom labels — catches concept-level entities
# (project, event, emotion, topic) that spaCy NER misses
gliner>=0.2.0
# dateparser: parses "last Tuesday", "two weeks ago" into datetime ranges
dateparser>=1.2.0

# Post-install: download required spaCy + coreferee English models
# python -m spacy download en_core_web_sm
# python -m coreferee install en

# Utilities
loguru>=0.7.2
tenacity>=8.2.3
schedule>=1.2.0
pytz>=2023.3
python-multipart>=0.0.6
jinja2>=3.1.2

# File Processing
PyPDF2>=3.0.1
python-docx>=0.8.11
openpyxl>=3.1.2
python-pptx>=0.6.21

# Monitoring and Metrics
prometheus-client>=0.19.0
